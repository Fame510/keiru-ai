import {createServerClient} from '@supabase/ssr';import {NextResponse,type NextRequest} from 'next/server';export async function updateSession(request:NextRequest){let r=NextResponse.next({request});const s=createServerClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,{cookies:{getAll:()=>request.cookies.getAll(),setAll:x=>x.forEach(c=>{request.cookies.set(c.name,c.value);r.cookies.set(c.name,c.value,c.options)})}});await s.auth.getUser();return r}