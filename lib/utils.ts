import {clsx,type ClassValue} from 'clsx';import {twMerge} from 'tailwind-merge';export const cn=(...x:ClassValue[])=>twMerge(clsx(x));export const money=(n:number)=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n);export const day=(x:string)=>new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric'}).format(new Date(x));export const apiError=(e:unknown,status=400)=>Response.json({error:e instanceof Error?e.message:'Request failed'},{status});