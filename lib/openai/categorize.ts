import {openai} from './client';export async function categorizeFinance(description:string){const r=await openai.chat.completions.create({model:'gpt-4o-mini',response_format:{type:'json_object'},messages:[{role:'system',content:'Return JSON {category:string} using a concise household budget category.'},{role:'user',content:description}]});return JSON.parse(r.choices[0].message.content||'{"category":"Other"}').category as string;}