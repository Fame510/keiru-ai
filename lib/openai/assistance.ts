import {openai} from './client';export async function suggestPrograms(profile:string){const r=await openai.chat.completions.create({model:'gpt-4o',response_format:{type:'json_object'},messages:[{role:'system',content:'Return JSON {programs:[{program_name,program_type,eligibility_score,application_url,deadline}]}. Suggest only well-known US public/nonprofit assistance programs. Use null for unknown URLs/dates.'},{role:'user',content:profile}]});const x=JSON.parse(r.choices[0].message.content||'{}');return Array.isArray(x.programs)?x.programs:[];}