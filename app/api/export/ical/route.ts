import {requireUser} from '@/lib/supabase/server';import {ical} from '@/lib/calendar/ical';export async function GET(){try{const {supabase,user}=await requireUser();const {data,error}=await supabase.from('events').select('*').eq('user_id',user.id);if(error)throw error;return new Response(ical(data||[]),{headers:{'content-type':'text/calendar; charset=utf-8','content-disposition':'attachment; filename="keiru-calendar.ics"'}})}catch{return Response.json({error:'Unauthorized'},{status:401})}}