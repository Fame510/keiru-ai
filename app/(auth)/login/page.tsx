'use client';import {createClient} from '@/lib/supabase/client';import {useState} from 'react';export default function Login(){const [email,setEmail]=useState(''),[msg,setMsg]=useState('');const s=createClient();async function magic(e:React.FormEvent){e.preventDefault();const {error}=await s.auth.signInWithOtp({email,options:{emailRedirectTo:`${location.origin}/api/auth/callback`}});setMsg(error?error.message:'Check your inbox for your secure sign-in link.')}return <main className="shell max-w-md py-24"><h1 className="text-4xl font-bold">Welcome to Keiru</h1><p className="mt-2">A little more support for every day.</p><form className="card mt-6 grid" onSubmit={magic}><label>Email<input className="field mt-1" required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><button className="btn btn-primary">Send magic link</button><button type="button" className="btn btn-secondary" onClick={()=>s.auth.signInWithOAuth({provider:'google',options:{redirectTo:`${location.origin}/api/auth/callback`}})}>Continue with Google</button>{msg&&<p className="text-sm">{msg}</p>}</form></main>}