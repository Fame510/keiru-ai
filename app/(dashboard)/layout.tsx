import {Nav} from '@/components/nav';import {createClient} from '@/lib/supabase/server';import {redirect} from 'next/navigation';export default async function DashboardLayout({children}:{children:React.ReactNode}){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)redirect('/login');const {data:p}=await s.from('users').select('onboarding_complete').eq('id',user.id).maybeSingle();if(!p?.onboarding_complete)redirect('/onboarding');return <div className="md:grid md:grid-cols-[220px_1fr]"><Nav/><main className="min-w-0">{children}</main></div>}