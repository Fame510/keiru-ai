import {cva,type VariantProps} from 'class-variance-authority';import {cn} from '@/lib/utils';import type {ButtonHTMLAttributes} from 'react';const v=cva('btn',{variants:{variant:{default:'btn-primary',secondary:'btn-secondary'},size:{default:'',sm:'px-3 py-1 text-sm'}},defaultVariants:{variant:'default',size:'default'}});export function Button({className,variant,size,...p}:ButtonHTMLAttributes<HTMLButtonElement>&VariantProps<typeof v>){return <button className={cn(v({variant,size}),className)} {...p}/>};