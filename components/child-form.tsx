'use client';import {useState} from 'react';export function ChildForm({onSaved}:{onSaved:()=>void}){const [name,setName]=useState('');return <form className="flex gap-2" onSubmit={async e=>{e.preventDefault();await fetch('/api/children',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name})});setName('');onSaved()}}><input className="field" value={name} onChange={e=>setName(e.target.value)} placeholder="Child name" required/><button className="btn btn-primary">Save</button></form>}